# Replit Deployment Configuration
# This file configures how the project should be built and deployed

[build]
run = "npm run build"
publicDir = "dist/public"

[deployment]
build = "npm run build && cp -r dist/public/* dist/ && rm -rf dist/public"
publicDir = "dist"